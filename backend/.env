# Application Environment
FLASK_ENV=production   # Could be 'development' or 'production' depending on your environment

# Database Configuration
DATABASE_URL=postgresql://postgres:Madou6282**@db:5432/unus_ai_bridge
POSTGRES_USER=postgres
POSTGRES_PASSWORD=Madou6282**
POSTGRES_DB=unus_ai_bridge

# Celery Configuration
CELERY_BROKER_URL=redis://redis:6379/0
CELERY_RESULT_BACKEND=redis://redis:6379/0

# Security Keys
SECRET_KEY=your-secret-key   # Replace 'your-secret-key' with an actual secret key for Flask

# JWT Authentication (if applicable)
JWT_SECRET_KEY=your-jwt-secret-key
JWT_EXPIRATION_DELTA=900   # in seconds (15 minutes)

# Redis Configuration
REDIS_URL=redis://redis:6379/0

# Application URLs
BASE_API_URL=http://localhost:5000/api

# Email Configuration (Optional, if email is used in your backend)
MAIL_SERVER=smtp.example.com
MAIL_PORT=587
MAIL_USE_TLS=True
MAIL_USERNAME=your-email@example.com
MAIL_PASSWORD=your-email-password

# Logging & Debug
LOG_LEVEL=INFO             # Could be DEBUG for development or INFO for production
